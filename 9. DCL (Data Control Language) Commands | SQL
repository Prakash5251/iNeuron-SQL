 DCL (Data Control Language) Commands:

1. GRANT:
   - Provides specific privileges to a user or a user role.
   ```sql
   GRANT privilege
   ON object
   TO user_or_role;
   ```

2. REVOKE:
   - Removes specific privileges from a user or a user role.
   ```sql
   REVOKE privilege
   ON object
   FROM user_or_role;
   ```

TCL (Transaction Control Language) Commands:

1. COMMIT:
   - Saves all the changes made during the current transaction.
   ```sql
   COMMIT;
   ```

2. ROLLBACK:
   - Undoes changes made during the current transaction.
   ```sql
   ROLLBACK;
   ```

3. SAVEPOINT:
   - Sets a point within a transaction to which you can later roll back.
   ```sql
   SAVEPOINT savepoint_name;
   ```

4. ROLLBACK TO SAVEPOINT:
   - Rolls back a transaction to a specific savepoint.
   ```sql
   ROLLBACK TO SAVEPOINT savepoint_name;




Video URL: https://youtu.be/rgTPuNeVFTc
PPT URL: https://docs.google.com/document/d/1FUK_a4phkpsfkqnR5wfrox2J6BwazBtkxU2YEBd5WAE/edit

